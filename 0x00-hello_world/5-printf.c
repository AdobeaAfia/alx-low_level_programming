#include <stdio.h>
/**
 * main - This is the main function prints a exactly what we want using printf
 * Return: 0 when successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
