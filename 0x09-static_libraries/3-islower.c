#include "main.h"
/**
 * _islower - function checks for lowercase character
 * @c: the desired character to check
 * Return: 1 if c is lowercase, 0 if c is not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
